# Tasks: add-hover-step-operation

## 1. The operation

- [x] 1.1 `host/agent/jev/questions.js`: add `OPERATIONS.HOVER`; include it in `TARGET_BEARING_OPERATIONS`; register its candidates in `buildActionSpace` beside `CLICK`'s (every offered element), and in the supported-operations list; update the header comment that enumerates per-operation candidate sets — done: `HOVER` after `SELECT` in the vocabulary order, target-bearing set updated, every element carries HOVER with CLICK's candidate record mirrored, supported list `[CLICK, TYPE_TEXT, SELECT, HOVER]`, comments updated ← (verify: only the vocabulary + candidates changed)
- [x] 1.2 `host/agent/jev/text-helper.js`: `NEXT_STEP` — `HOVER` in the operation enumeration and in the intent-required enumeration, plus one line: hover-only menus/tooltips open on pointer rest, a click on such a control does nothing, hover the intent-named control then CLICK an item the menu then offers; update the request/doc comment that names `CLICK`/`TYPE_TEXT`/`SELECT` as the selection-bearing operations — done; validator logic unchanged. Post-verify fix (agent://VerifyHoverStep F1, low): the `requestStepDecision` JSDoc's selection-bearing list now names `HOVER` too ← (verify: no validator loosened)
- [x] 1.3 `host/agent/jev/runtime.js`: `KIND_BY_OPERATION` gains `hover`; the dispatch chain gains `HOVER` → `{ tool: "computer", args: { action: "hover", ref: target.ref, tabId } }`; the header comment's dispatch table gains the `HOVER` row — done; repeated-click guard stays CLICK-only ← (verify: terminal semantics and gates unchanged)
- [x] 1.4 `extension/sidepanel/tool-labels.js`: `hover: "Đã di chuột"` beside the other computer-action labels — done. Post-verify fix (agent://VerifyHoverStep F2, info): the JEV operation label map gains `HOVER: "Đã di chuột"` as well, so the step row reads Vietnamese like its siblings; `test/sidepanel-conversation-model.test.mjs` re-run green after the edit ← (verify: only the vocabulary + candidates changed)

## 2. Tests

- [x] 2.1 `host/test/jev-questions.test.mjs`: `TARGET_BEARING_OPERATIONS` includes HOVER; `buildSelectionRequest` with `operation: "HOVER"` builds the `hover_target` head with exactly the CLICK candidate set; the supported-operations list advertises HOVER when elements are offered — done: per-element sets are CLICK,HOVER[,TYPE_TEXT][,SELECT]; the new test pins head name, criteria keys == CLICK keys, and HOVER in `operations` ← (verify: only the vocabulary + candidates changed)
- [x] 2.2 `host/test/jev-text-helper.test.mjs`: `parseStepDecision` accepts `{operation:"HOVER", intent:"…"}`; refuses HOVER without `intent` (target-bearing rule); refuses `text`/`url` on HOVER with the existing messages; `NEXT_STEP` names HOVER and the hover-menu guidance — done ← (verify: no validator loosened)
- [x] 2.3 `host/test/jev-runtime.test.mjs`: a HOVER decision → `hover_target` selection → exactly one dispatched `computer` call with `action: "hover"` and the selected `ref` (no click); the step records kind `hover`/argsSummary; no approval prompt for the hover; a following CLICK of a revealed item dispatches normally (the dauthau menu shape) — done: both shapes green (hover alone: one bridge dispatch, zero approval cards, kind hover in `recent_actions`; hover-then-click: dispatches [hover ref_1, left_click ref_3], the click's selection runs on the opened menu, one approval card bound to the click only) ← (verify: terminal semantics and gates unchanged)

## 3. Focused suites and validation

- [x] 3.1 `cd host && node --test test/jev-questions.test.mjs test/jev-text-helper.test.mjs test/jev-runtime.test.mjs test/jev-client.test.mjs` green; from root `node --test extension/sidepanel/tool-labels.test.mjs` if present (else skip); `openspec validate add-hover-step-operation --strict` valid — done: 4 files pass, 0 fail (32/32, 46/46, 80/80, 21/21); validate valid; the gate classifier and the label were smoke-checked directly in node ← (verify: green; only the owned files edited)

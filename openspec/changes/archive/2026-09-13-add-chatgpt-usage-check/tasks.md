## 1. Host usage module (`host/agent/chatgpt/usage.js`)

- [x] 1.1 Create `host/agent/chatgpt/usage.js` with `createUsageReader({ fetchImpl, getAccessToken, loadProfile, onSessionExpired, now, upstreamUrl, timeoutMs })` returning `{ readUsage(profileId) }`; defaults resolve the real `chatgpt/auth.js`, `settings/profile.js` (`loadProfile`, `recordChatgptSessionExpired`) and the verified endpoint; module header names `router-for-me/CLIProxyAPI` as the provenance of the endpoint knowledge (same pattern as `upstream-errors.js`).
- [x] 1.2 Eligibility first: `loadProfile()` and return `{ ok:false, error:{ code:"SESSION_EXPIRED" } }` for `chatgptSessionState === "session_expired"` and `{ code:"NO_CREDENTIAL" }` when not `signed_in` or `hasCredential` is false — no network call in either case.
- [x] 1.3 Request and mapping: `GET <upstreamUrl>` with only `Authorization: Bearer <accessToken>` and `chatgpt-account-id: <accountId>` from `getAccessToken(profileId)`; map `plan_type` → `planType`, `rate_limit.allowed`/`limit_reached` → `allowed`/`limitReached`, `primary_window`/`secondary_window` → `primary`/`secondary` with `{ usedPercent, limitWindowSeconds, resetAfterSeconds, resetAt }` (each `null` when absent); normalize `reset_at` to epoch ms from an epoch-seconds/ms number or an ISO-8601 string, `null` otherwise; `credits` present as `{ hasCredits, unlimited, balance }` only when `hasCredits` is true, else `null`; no other backend field is copied.
- [x] 1.4 Auth rule and failures: on 401 call `getAccessToken(profileId, { forceRefresh: true })` once and retry once; a surviving 401 awaits `onSessionExpired(profileId)` and returns `{ code:"SESSION_EXPIRED" }`; a rejected refresh (`NO_CREDENTIAL`/`SESSION_EXPIRED`/other `ProviderError`) is translated, never recorded twice; map 429 → `RATE_LIMIT_ERROR`, 403 → `AUTH_ERROR`, other non-2xx → `NETWORK_ERROR` with the status in the message, fetch rejection/`AbortSignal.timeout` via `classifyNetworkError`, and a 200 body without a `rate_limit` object → `USAGE_UNAVAILABLE`. `readUsage()` resolves `{ ok:false, error }` for every one of these and never throws. ← (verify: one and only one forced refresh per read; the retry uses the refreshed token; a no-credential read performs zero fetches; no access token, account id, user id, or email appears anywhere in the result or error objects)
- [x] 1.5 Add `USAGE_UNAVAILABLE` to `PROVIDER_ERROR_CODES` in `host/agent/settings/errors.js` with a one-line comment (a 200 body that is not the expected usage shape — distinct from `PROTOCOL_ERROR`, which the settings page reads as "update the companion").
- [x] 1.6 New `host/test/chatgpt-usage.test.mjs` (self-contained `node host/test/chatgpt-usage.test.mjs`, `PASS` lines, exit 0) with a mock fetch and mock auth: single-window free-plan mapping, both-window paid mapping, absent `reset_at`, missing `rate_limit` → `USAGE_UNAVAILABLE`, 401 → refresh → retry success (exactly one refresh call, retry carries the fresh token and the `chatgpt-account-id` header), surviving 401 → `SESSION_EXPIRED` with exactly one `onSessionExpired` call, refresh-rejection path with no second transition, 429/403/5xx/fetch-rejection/timeout mapping, no-credential and session-expired short-circuits assert zero fetches, and a leak assertion over the whole result/error tree. ← (verify: every spec scenario of "ChatGPT account usage read" has a case; run the file and confirm exit 0)

## 2. Settings protocol (`chatgpt_usage`)

- [x] 2.1 `host/agent/companion.js`: add `case "chatgpt_usage"` to `_handleAgentSettings()` — validate `profileId` (fail `PROTOCOL_ERROR` when missing, matching the sibling ChatGPT ops), resolve the reader lazily through a new `_getChatgptUsageReader()` with an injectable `chatgptUsageReader`/provider constructor option for tests, answer `ok(result)` / `fail(code, message)` from the module's structured result, and pass the success result through `_assertAgentSettingsResultSecretFree()`.
- [x] 2.2 `host/test/agent-settings-relay.test.mjs`: extend the ChatGPT op-family coverage to `chatgpt_usage` (relay allowlist + companion reply shape), asserting the success result carries only the six usage keys (`planType`, `allowed`, `limitReached`, `primary`, `secondary`, `credits`) and that a failing read returns `{ok:false,error:{code}}` in the settings envelope rather than a thrown error. ← (verify: run the file and confirm exit 0; assert the reply's key set exactly, so a future backend field cannot slip through)

## 3. Extension wire and relay

- [x] 3.1 `extension/settings/settings-client.js`: add `chatgptUsage: (profileId) => call("chatgpt_usage", { profileId })` and extend the file-header wire-contract block with the op's request/reply shape (including that the reply carries no token and no account identity).
- [x] 3.2 `extension/background.js`: add `chatgpt_usage` to `createAgentSettingsRelay()`'s `ops` set (no cache-mirror entry — the op changes no profile field).
- [x] 3.3 `extension/settings/errors-ui.js`: add `chatgpt_usage` to `CHATGPT_OPS` and Vietnamese copy for `USAGE_UNAVAILABLE` (title/message/action, matching the existing code-copy style).
- [x] 3.4 `test/settings-ui-client.test.mjs` and `test/background-agent-settings-relay.test.mjs`: assert the outgoing `{type:"agent_settings", op:"chatgpt_usage", profileId}` shape, and that the relay accepts the op while still rejecting an unlisted one. ← (verify: run both files, exit 0; the relay test's per-family allowlist list must include `chatgpt_usage`, and the unlisted-op rejection case must still pass)

## 4. Settings UI (ChatGPT section)

- [x] 4.1 `extension/settings/settings-controller.js`: add `usage` state (`{ status: "idle"|"loading"|"ready"|"error", usage, error }`) and `refreshUsage()`; start one read from `init()`/`_applyProfile()` only when the profile is `chatgpt` and `signed_in`; tag each read with the profileId it was issued for so a late response cannot land on another profile; compute each window's absolute reset moment from the injectable `now`; never poll on a timer.
- [x] 4.2 `extension/settings/settings-app.js`: render plan, one line per present window (percent used + live countdown from the stored reset moment, single `setInterval` cleared when the block is hidden), the credits line only when present, a limit-reached line when `limitReached`, the refresh button, and loading/error/`aria-live` text from `errors-ui.js` copy; a failed read leaves the account email, plan and model list display untouched and keeps refresh available.
- [x] 4.3 `extension/settings/settings.html`: add the usage block markup and CSS inside the existing `#chatgpt-fields` region (no inline script; the CSP guard must stay green) with labels wired to the app renderer.
- [x] 4.4 `test/settings-ui-controller.test.mjs`: cases for load-then-read on a signed-in `chatgpt` profile, no read for an `anthropic` profile / signed-out profile / `session_expired` profile, explicit refresh replacing values, failure landing in `usage.error` while account and models stay intact, discarded response after a profile switch, and no timer-driven read. ← (verify: run the file, exit 0; the "no read when not signed in" cases must assert zero client calls)
- [x] 4.5 `test/settings-ui-secrets.test.mjs`, `test/extension-scripts-parse.test.mjs`, `test/extension-csp-no-inline-scripts.test.mjs` stay green with the new markup/scripts, and the secrets test's state/DOM scan covers the usage block (no token, no account id). ← (verify: run all three files, exit 0 — this is the security-relevant end of the extension flow)

## 5. Live opt-in check and docs

- [x] 5.1 Extend `host/test/chatgpt-live.test.mjs` (still skipped unless its env var is set) with one real `chatgpt_usage` read through the companion op path, asserting the shape renders (`planType`, at least one window with a percent) without printing a token, account id, or email.
- [x] 5.2 `README.md`: one paragraph in "Run it on a ChatGPT subscription" stating that Settings shows the account's current usage and limits (percent per window with a reset countdown and a refresh action), that it reads the same signed-in account, and that the read does not consume the model quota.
- [x] 5.3 `docs/cai-dat.md`: the Vietnamese equivalent in the ChatGPT section, with the same limits wording (undocumented backend; failures are contained to the usage block). ← (verify: docs match the shipped copy and claims — no promise the endpoint is official or always available)

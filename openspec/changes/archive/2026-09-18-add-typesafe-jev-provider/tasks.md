# Tasks: TypeSafe Jev Provider (Ultrafast Runtime)

## 1. Registry: `page_snapshot` operation bookkeeping

- [x] 1.1 Add the `page_snapshot` entry to `host/tool-definitions.js` (input `{ tabId }`, description stating it returns a bounded structured snapshot — visible/enabled interactive controls with references usable by `computer`/`form_input`/`scroll_to` plus bounded visible text — and that reads are read-only), and update the file header's tool-count comment to the new totals (25 preserved + 7 post-baseline − 1 removed = 31).
- [x] 1.2 Classify `page_snapshot` as read-only in `host/agent/tools/mapping.js` (`READ_ONLY_LEGACY_TOOLS`, and any live-read set that enumerates read tools), give it an SDK-facing description following the `mask_sensitive_info` precedent, and leave `SEND_CLASS_TOOL_NAMES` untouched.
- [x] 1.3 Update `test/registry-baseline.test.mjs`: add `page_snapshot` to `POST_BASELINE_ADDITIONS` and keep the count assertions, header-consistency check, and legacy `*mcp*` handling coherent.
- [x] 1.4 Update `extension/events/action-events.js` so `page_snapshot` maps to the read action type and a human-readable label (follow the `read_page` mapping), and update `extension/background.js`'s `shortcutStepProducesContent` list only if read tools are enumerated there (check `test/shortcut-prove-handler.test.mjs`; keep its self-contained extraction property intact).
- [x] 1.5 Update README registry counts everywhere they appear (intro, comparison table, architecture, hybrid-server paragraph) to the new totals, adding `page_snapshot` to the post-baseline enumeration and its one-line explanation.
- [x] 1.6 Run `node test/registry-baseline.test.mjs`, `node test/action-events-schema.test.mjs`, `node test/shortcut-prove-handler.test.mjs` and fix only in-scope failures. ← (verify: header counts, baseline test, and README all state the same totals; `page_snapshot` appears exactly once in the post-baseline set; no preserved operation was touched)

## 2. Extension: `page_snapshot` implementation

- [x] 2.1 In `extension/content.js`, add a `pageSnapshot` message handler and builder that reuses the existing internals (`getRole`, `getAccessibleName`, `isVisible`, `isInteractive`, `getOrAssignRef`, masked-value handling, select options) and returns the contract from design.md §2: `{ v, url, title, viewport, scroll, text, truncated: { elements, text, omitted }, elements: [...] }` — visible and enabled interactive controls only, ≤250 elements, labels/values ≤100 chars, text ≤6000 chars, masked controls as `"••••••"`, each element carrying `ref`, `role`, `label`, `tag`, `type`, `value`, `editable`, `readonly`, `contenteditable`, `disabled`, `checked`, `selected`, `expanded`, and `options` for native selects.
- [x] 2.2 In `extension/background.js`, add the `page_snapshot(args)` handler: group membership + readability checks like `read_page`, forward `{ type: "pageSnapshot", ... }` to the tab, serialize the result as the tool's text content, and add the tool name to whatever routing/name lists the dispatch switch requires (mirror how `read_page` is wired). Ensure the operation never sends input to the page.
- [x] 2.3 Add `test/page-snapshot-content.test.mjs` and `test/page-snapshot-background.test.mjs` following the existing structural-proof patterns (`test/_extract.mjs`'s `extractMethod`), asserting: the builder filters to visible+enabled controls, applies the bounds and truncation disclosure, masks masked values, includes select options and editable flags, and that the background handler performs the group/readability checks before dispatch and performs no page mutation.
- [x] 2.4 Run `node test/page-snapshot-content.test.mjs`, `node test/page-snapshot-background.test.mjs`, plus the touched adjacent suites. ← (verify: every contract field is produced; no write path exists in the operation; refs are the same ref space the existing tools resolve)

## 3. Host: single-sourced dispatch checks

- [x] 3.1 Extract `runHostSideChecks` and its private helpers (`authorizationErrorResult`, `borrowedTabMutationErrorResult`, `staleApprovalErrorResult`, `_isAutoAuthorizeEligible`, `_tabIdsForArgs`) from `host/agent/tools/adapter.js` into a new `host/agent/tools/dispatch-checks.js`, exported for reuse; adapter imports them. This is a behavior-preserving move: no logic edits, exports untouched, comments preserved or updated only for the new location.
- [x] 3.2 Add `host/test/dispatch-checks.test.mjs` covering the extracted module directly (run-state rejection, lease rejection, tab-scope rejection, protected backstop with grant/verdict coverage, send-class peek-vs-spend semantics), and confirm `host/test/agent-tool-adapter.test.mjs`, `host/test/protected-action-gate-hook.test.mjs`, and every adapter-touching suite remain green without edits.
- [x] 3.3 Run the host test suite files touching adapter/dispatch. ← (verify: a single copy of the checks exists; adapter behavior unchanged (existing tests pass unmodified); grep proves no duplicated checks remain in adapter.js)

## 4. Host: the Jev runtime modules

- [x] 4.1 `host/agent/jev/questions.js`: build the numbered element table and index→ref map from one `page_snapshot` payload; derive supported operations per element (`CLICK`, `TYPE_TEXT`, `SELECT` per option, `SCROLL_UP`/`SCROLL_DOWN`, `WAIT`, `DONE`, `BLOCKED`) offering only what the observation supports; build the `questions` payload (operation head + one target head per offered operation) with ported-and-adapted `NEXT_ACTION`/`TARGET` instructions per design.md §3.
- [x] 4.2 `host/agent/jev/client.js`: `POST {endpoint}/v1/systemone` with Bearer auth; strict validation (choice membership, probability key coverage, finite [0,1] values, sum≈1, declared choice is maximum); retry policy for 429/503/529 (≤2 retries, 0.5·2^n); classified failures (`AUTH_ERROR`, `RATE_LIMIT_ERROR`, `MODEL_UNAVAILABLE_ERROR`, `TIMEOUT_ERROR`, `NETWORK_ERROR`, `INVALID_RESPONSE`); returns `{ decision, usage, latencyMs }` with no side effects.
- [x] 4.3 `host/agent/jev/text-helper.js`: `POST {textModel.baseUrl}/chat/completions` (model, `max_tokens: 1024`, `response_format: json_object`, the deepseek/other reasoning rule from design.md §5); parse exactly `{ text }`; `null`/malformed/oversize ⇒ typed failures distinct from transport failures.
- [x] 4.4 `host/agent/jev/runtime.js`: the bounded loop per design.md §6 — observe via `toolBridge.call("page_snapshot", …)` (args coerced with the injected `coerceArgs`, meta via `run.describeRequestForWire()`); one decision per cycle; execution mapping to `computer`/`form_input`; pre-dispatch gates through `dispatch-checks.js` and the reused `createCanUseTool` wiring; `recordResultUnknown` + terminal `tool_result_unknown` on lost responses; stop honored between phases; no-progress and budget ends; `jev_step`/`jev_end` emission via `run.emit`.
- [x] 4.5 `host/agent/jev/capability.js`: the two-stage capability test (trivial systemone choice question + minimal text-model completion), returning per-stage outcomes and the same recorded-result shape the settings layer stores.
- [x] 4.6 Tests: `host/test/jev-questions.test.mjs` (action-space/URL-shape/instructions invariants), `host/test/jev-client.test.mjs` (validation matrix + retry policy against a stub HTTP server), `host/test/jev-text-helper.test.mjs`, `host/test/jev-capability.test.mjs`, and `host/test/jev-runtime.test.mjs` with a fake tool bridge and real `Run`/`ApprovalRegistry` objects covering: happy path; approval allow/deny/timeout; borrowed-tab rules; stop between steps and mid-dispatch; both budgets; no-progress; result-unknown; observation failure; invalid decision; missing value; and the invariant that no dispatch occurs without passing the shared checks.
- [x] 4.7 Run the new host jev suites plus `host/test/dispatch-checks.test.mjs`. ← (verify: runtime matrix complete; model output can only ever select offered numbers (no selectors/coordinates/scripts); every terminal outcome emits `jev_end`; no action dispatches without the shared checks)

## 5. Settings: the TypeSafe provider type

- [x] 5.1 `host/agent/settings/profile-schema.js`: add `"typesafe"` to `PROVIDER_TYPES` (load-path behavior for legacy/unknown values unchanged).
- [x] 5.2 `host/agent/settings/profile.js`: `snapshotForRun` branch returning the design.md §1 shape (keys only under `typesafe`/`textModel`; `env` carries only the `typesafe:jev` marker; `runtime: "typesafe"`); `testCapability` branch via `jev/capability.js`; `isRunnable` accepts `typesafe`; `refreshDiscoveredModels` reports unsupported; seed `jev-latest` when a switch to `typesafe` finds an empty list; `setTypesafeConfig` and `setTypesafeCredentials` (merged JSON secret `{v:1, typesafe_api_key, text_model_api_key}` at `browzy-in-chrome/typesafe/<profileId>`, memory-only option, `SECRET_TOO_LARGE` failure, revocation on removal).
- [x] 5.3 `host/agent/settings/errors.js`: add `INVALID_RESPONSE`; `extension/settings/errors-ui.js`: Vietnamese copy for it and for the TypeSafe failure stages.
- [x] 5.4 `host/agent/companion.js` (`CompanionCore._handleAgentSettings`, where every other `agent_settings` op is handled): wire `set_typesafe_config` and `set_typesafe_credentials`; accept `typesafe` in `set_provider_type`'s value check; ensure replies never include raw keys and `get_profile` exposes only non-secret fields (has-key booleans).
- [x] 5.5 Extension settings surface: `extension/settings/settings-client.js` methods; `extension/background.js` relay allowlist for the two new ops; `extension/settings/settings-controller.js` + `settings.html` + `settings-app.js` for the third provider option (write-only TypeSafe key, text-model base URL/model/key fields, disclosure that TypeSafe and the text-model service are separately billed, test control enabled only when both keys are saved and text config validates; Anthropic Base URL/API-key fields hidden for this type).
- [x] 5.6 Tests: `host/test/settings-typesafe.test.mjs` using a stub TypeSafe/text-model server (snapshot shape; keys never in env/profile/export/log strings; capability stages and `INVALID_RESPONSE`; seeding-on-switch; `isRunnable`; credential merge/removal/revocation; protocol ops), and update the extension settings UI tests for the new controller state fields and gating.
- [x] 5.7 Update README's provider configuration section with the TypeSafe option. ← (verify: no secret leaves the companion in any reply/export/log path (grep-backed); the capability test distinguishes its two stages; `anthropic`/`chatgpt` suites unmodified and green)

## 6. Companion wiring

- [x] 6.1 `host/agent/companion.js`: in `_runAfterLeaseGranted`, after `resolveProfileSnapshot()` and the identity-compatibility gate, branch `snapshot.runtime === "typesafe"` to `_runTypesafe(...)`, which (a) rejects attachments, `elementRecord`, slash dispatch, and upload grants with the single run-error reason `unsupported_in_typesafe_mode` naming the field, (b) skips skills binding, (c) still performs the conversation app-profile binding with the marker baseUrl, (d) constructs the runtime with injected `toolBridge`, `coerceArgs`, `run`, the approval machinery (`approvals`, request-id tracker, and the same `resolveHint` wiring the SDK path uses), and the resolved provider config, and (e) emits `run_error` reasons from the runtime's classified failures. The SDK path (`_runQuery` and everything it builds) must not change.
- [x] 6.2 `host/agent/protocol.js`: name and document the durable event kinds `jev_step`/`jev_end` (not transient), following the existing THREAT_EVENT_TYPES documentation style.
- [x] 6.3 Tests: extend the companion harness suites for the branch order, each rejection reason, identity binding, a happy-path run with fakes, stop semantics, and lease/queue arbitration between a typesafe run and an LLM run.
- [x] 6.4 Run the companion suites. ← (verify: branch placement after the compat gate proven; SDK path diff is zero; every rejection reason is exercised; stop/lease behavior matches existing runs)

## 7. Panel: Jev visibility

- [x] 7.1 `extension/sidepanel/conversation-model.js`: handle `jev_step` (append a toolRows entry — row name `jev_<operation>`, target label, probability/confidence, executed tool args summary, pageChanged, latencies; skipped/rejected steps represented honestly) and `jev_end` (terminal outcome line: done-as-decided, or blocked with its reason); ensure response/rebuild from the transcript produces identical rows (idempotent under the existing full-rebuild rule).
- [x] 7.2 `extension/sidepanel/sidepanel.js` + `tool-labels.js`: render the outcome line under the turn, resolve the busy indicator for text-less Jev runs, and add Vietnamese labels for jev row kinds and outcomes.
- [x] 7.3 Tests: extend `test/sidepanel-*.test.mjs` suites (conversation-model live application + snapshot rebuild equality, jev outcome rendering, stopped/failed labelling, no fabricated assistant text).
- [x] 7.4 Run the panel suites. ← (verify: live and rebuilt states are identical; a text-less run ends in a resolved state; no duplicate steps after reconnect)

## 8. Docs and full-suite close-out

- [x] 8.1 README: registry totals (already in 1.5), the TypeSafe provider section, and the side-panel status note where provider behavior is described.
- [x] 8.2 Run the complete offline suites (`node test/*.test.mjs` at the repo root, `node host/test/*.test.mjs`, plus each new suite) and `openspec validate add-typesafe-jev-provider`; fix only in-scope failures. ← (verify: all listed suites green; openspec validation passes; the change's tasks are fully checked off)

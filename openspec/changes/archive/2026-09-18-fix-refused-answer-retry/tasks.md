# Tasks: fix-refused-answer-retry

## 1. One feedback retry in the shared builder

- [x] 1.1 `host/agent/jev/text-helper.js`: `postMemoryRequest` retries exactly once when the parse/validator refuses the answer — the second request re-sends the same body plus the refused answer (assistant) and a corrective user turn carrying the refusal reason and "Reply with ONLY the JSON object the instruction requires — no prose, no markdown fences."; a second refusal throws today's `JevError` with `attempts: 2` in the detail. Only validation refusals consume the retry (transport policy untouched) — done: bounded at 2 attempts; a postJson throw propagates untouched; the retry re-sends the identical body (user message with image) plus the assistant content verbatim and the corrective turn; second refusal keeps the code and adds `{attempts: 2, replyPreview, replyEmpty}` with latency summed over attempts ← (verify: refused-answer semantics; nothing else changed)
- [x] 1.2 `host/agent/jev/text-helper.js`: `parseMessageObject` unwraps a leading/trailing ```` ``` ```` fence (the opening fence line and the closing fence) before the strict parse; no other extraction. The thrown refusal message gains a bounded preview of the model's reply ("reply starts: …" / "the reply is empty") and the detail carries the preview with `attempts` — done: `stripFence` (leading ```/```json line + trailing ``` line only, unclosed fence still unwraps; prose inside/before/after still refuses); `contentRefusal`/`replyPreview` (120 chars, whitespace-collapsed, `…`-marked); every other refusal prefix and code unchanged; module header + JSDoc updated ← (verify: refused-answer semantics; nothing else changed)

## 2. Tests

- [x] 2.1 `host/test/jev-text-helper.test.mjs`: prose-then-valid answer succeeds with exactly two requests and the retry request carrying the refused content + the corrective turn; a fenced-but-valid object parses on the FIRST attempt with no retry; prose twice throws `INVALID_RESPONSE` with `attempts: 2` and the preview in message + detail; a validator refusal (unknown operation) is retried identically; a transport abort keeps exactly the transport's own policy (no content retry) — done: 8 new tests, including the image-carrying retry body, the same-refusal retry for unknown operation + missing value, fence cases (json/bare/unclosed/prose-around), the 120-char `…` cut, the empty reply, and the transport separations (500 → 1 request; aborting fetch → exactly the transport's one repeat) ← (verify: refused-answer semantics; nothing else changed)
- [x] 2.2 `host/test/jev-runtime.test.mjs`: a step decision that returns prose first and a valid decision second executes the step (the live failure recovers); prose twice ends `error` / `invalid_decision` with the preview in the recorded message and no step fabricated — done: both cases green (prose-then-valid: done, 1 executed step, 3 NEXT_STEP requests total with the retry body carrying the same step context; prose-twice: 2 attempts, 0 dispatches, 0 TypeSafe requests, 0 fabricated steps, one `jev_end`) ← (verify: terminal semantics unchanged)

## 3. Focused suites and validation

- [x] 3.1 `cd host && node --test test/jev-text-helper.test.mjs test/jev-runtime.test.mjs test/jev-client.test.mjs test/jev-questions.test.mjs` green; `openspec validate fix-refused-answer-retry --strict` valid — done: 44/44, 78/78, 21/21, 31/31 passed; validate valid; only the three owned files edited ← (verify: green; only the owned files edited)

## 1. Classification and mode resolution

- [x] 1.1 Extend the classifier so every registry tool and every `computer` action resolves to exactly one of read-only, mutating, send/submit-class, or protected, reusing the existing exhaustive read-only/mutating sets rather than adding a parallel classification.
- [x] 1.2 Make an unclassifiable action and a registry entry with no classification resolve to protected, and report the missing entry as a classification gap naming the tool.
- [x] 1.3 Add the mode resolver: a pure function from (class, mode, per-site match, managed policy) to "requires decision" or "proceeds", with the fixed one-directional order — protected, then managed policy, then per-site store, then mode — where no later stage can remove a requirement an earlier stage imposed.
- [x] 1.4 Unit test the resolver over every registry entry for all three modes, asserting Auto reproduces today's verdict for every entry. ← (verify: the Auto column of the test matches the pre-change behavior exactly, entry by entry, so the change is provably inert until a mode is chosen)
- [x] 1.5 Extend the registry-baseline test so a tool added without a classification entry fails the suite. `implement-stubbed-browser-tools` has completed and archived, freeing `test/registry-baseline.test.mjs` for this change. Added a "read-only/mutating classification coverage" section there (imports `_mutationClassificationCoverage()` from `host/agent/tools/mapping.js` and re-checks it against the LIVE `TOOLS` array, which this suite already re-imports fresh on every run): asserts every live tool name is covered by exactly one of `READ_ONLY_LEGACY_TOOLS`/`MUTATING_LEGACY_TOOLS`, or is `"computer"` (classified per-action, not by name). This closes the exact gap `host/test/permission-modes.test.mjs`'s own coverage check cannot: that suite only proves the CURRENT two sets have no gap against the CURRENT registry, and never re-runs just because `tool-definitions.js` grows a new entry with no matching mapping.js update — `isMutatingCall`'s fail-safe mutating default means such an omission previously classified silently instead of failing. Verified: reverting the classification sets to omit one live tool name makes this new check fail with a named discrepancy (manually confirmed, then reverted).

## 2. Protected actions

- [x] 2.1 Add the protected-action check to the unconditional handler-side authorization path, positioned so no mode, remembered decision, or SDK preapproval can bypass it.
- [x] 2.2 Classify credential and payment-detail entry as protected, naming the category in the decision it raises.
- [x] 2.3 Classify granting a browser permission on a page's behalf as protected, naming that category.
- [x] 2.4 Add the `downloads` permission to the manifest and observe download creation, classifying an agent-caused download as protected and naming that category. Use the permission only for observation.

  DONE (extension wave). `extension/manifest.json` now declares `downloads`. `extension/background.js` adds `chrome.downloads.onCreated.addListener(handleDownloadCreated)`, gated on `isAgentRunActive()` (a run currently holds the browser lease — tracked in the SAME two places `overlayRunTabs` already is: `run_started` / `OVERLAY_TEARDOWN_RUN_EVENTS`) so a user's own manual download when no run is active is never touched.

  Architecture note, stated plainly rather than glossed over: this does NOT route through `host/agent/policy/can-use-tool.js`'s approval-token machinery, and could not — `chrome.downloads.DownloadItem` carries no `tabId`, and a download is a passive browser event, not a dispatched tool call with an action/target/document-identity for that machinery's per-call binding to verify against. There is nothing in `host/**` for this event to reach at all (host has no browser access and cannot see `chrome.downloads`). The gate is therefore a fully LOCAL, extension-only decision surface: on creation, `handleDownloadCreated` calls `chrome.downloads.pause()` immediately, then broadcasts a synthetic `download_protected_decision` envelope (own top-level type, never added to `host/agent/protocol.js`) naming category `"download"` and `rememberable:false` to connected panels; the panel's Allow/Deny reply (`download_decision`, also local-only) is intercepted in `background.js`'s `ocic-agent` port handler BEFORE it would ever reach `nativePort`, and resolves to `chrome.downloads.resume()`/`cancel()`. Never used to start, redirect, or read a download's content.

  Honestly disclosed limit (matches this task's own wording): a download that completes before `pause()` can take effect (`item.state === "complete"` on creation, or `pause()` itself reporting `chrome.runtime.lastError`) is reported via a `download_notice` envelope/transcript item stating `outcome: "completed_before_pause"` — never presented as if it had been gated.

  Covered by `test/background-managed-policy-and-downloads.test.mjs` (the gate/pause/resume/cancel/notice logic, extracted from the shipped `background.js`) and `test/permission-mode-panel-wiring.test.mjs` (the panel-side card/routing). Not integration-tested against a real Chrome `chrome.downloads` implementation — that needs the actual browser, per this project's existing testing posture for every other `chrome.*`-dependent handler in this file.

  Follow-up fix (CRITICAL, closed this pass): the outstanding decision this gate raises used to be cleared ONLY when the user answered it — nothing invalidated it on Stop, on a permission mode change, or on the run it belonged to ending any other way, so a late Allow after the user had already walked away still called `chrome.downloads.resume()`. Fixed with no change to the gate itself: `pendingDownloadDecisions` entries now carry the `runId`/`conversationId` active when the download was paused; `background.js`'s existing `OVERLAY_TEARDOWN_RUN_EVENTS` hook (the same one that already tears down the overlay and clears `activeAgentRuns`) now also calls a new `invalidateDownloadDecisionsForRun()`, deleting the entry (never resuming/cancelling — the download is simply left paused, exactly as an invalidated decision should be) and broadcasting `download_decision_invalidated` to every connected panel; a permission-mode change (which has no run boundary to key off) is invalidated the same way via a new `invalidateAllDownloadDecisions()`, triggered by `PanelController.invalidateAllPendingApprovals()` sending a new LOCAL-only `download_decisions_invalidate` message (mirroring `download_decision`'s own never-reaches-nativePort rule). `ConversationModel` independently clears its own `pendingDownloadDecision` from the identical run-teardown `stream_event` it already applies, so neither side depends on a round trip through the other for that case. Also closed: this protected decision's OUTCOME was invisible to the durable conversation record once the panel session ended (unlike every other protected decision, whose outcome is an ordinary `tool_result`/`tool_rejected` the host already persists) — `background.js` now sends a fire-and-forget `download_decision_recorded` envelope (new `AGENT_MESSAGE_TYPES` entry, `host/agent/session/manager.js`'s `recordDownloadDecision()`) after every resume/cancel, appended to that conversation's transcript and rendered as a `download_decision` item (`ConversationModel.recordDownloadDecision()`/its `applyEvent()` replay case) the instant the user answers, not only on a later reconnect. Covered by `test/background-download-decision-invalidation.test.mjs` (invalidation + durable-record relay, extracted from shipped `background.js`) and `host/test/download-decision-durable-record.test.mjs` (the companion-side append/idempotency/gating).
- [x] 2.5 Classify host-initiated file writes as protected under the same category. Decision criterion applied: a host-initiated write is FILE_WRITE-protected when it leaves a file that PERSISTS FOR THE USER beyond the operation that made it; a temp file that exists only to carry out that same call, and is not left behind as a user artifact, is not a separate protected write. Checked every host-initiated write an agent tool call can actually reach:
  - `computer` screenshot with `save_to_disk:true` and `gif_creator` export with `download:true` both write a file the user asked to keep (`save_screenshot_to_disk`/`save_gif_to_disk` in `host/native-host.js`) — both already classified FILE_WRITE in `detectProtectedCategory`.
  - `write_temp_file` (`host/native-host.js`) — read its actual behavior: it stages one file under the app's own `~/.config/browzy-in-chrome/tmp/` directory purely so `upload_image`/`file_upload`'s CDP drag-drop/`setFileInputFiles` calls have a filesystem path to hand the browser; nothing in `extension/background.js` ever tells the user this path exists or invites them to keep it, it is not written to a user-chosen or user-visible location, and it is garbage-collected by the SAME handler after 24h. This is exactly the excluded case: the protection concern for `upload_image`/`file_upload` is the call itself (uploading a file to a page), which is an ordinary mutating DOM interaction, not a second, independent file-write concern.
  - `save_recording`/`save_audio`/`save_screenshot` (recorder frames) and `save_audit` (`extension/audit/index.js`) DO write durable, user-visible files, but none is reachable from an agent tool call at all — they are the human-initiated screen-recording and audit-logging features, driven entirely by `extension/background.js`'s own recorder/audit state machines, never by anything in `host/tool-definitions.js`'s `TOOLS` registry. `detectProtectedCategory`/`classifyActionClass` classify AGENT actions (`legacyToolName` from the registry); a write with no agent-tool call in its call chain is outside what this classifier can or should gate — there is no action for a permission mode to ask about.
  No code change was needed: the only host-initiated writes an agent action can trigger that leave a durable user artifact were already covered.
- [x] 2.6 Make protected decisions non-rememberable: no per-site entry is created, and an existing entry is not consulted. ← (verify: a protected action still prompts under Skip mode with a matching remembered allowance present — the case where every bypass path is simultaneously open)

## 3. Per-site permission store

- [x] 3.1 Add the store keyed by origin and action class, recording allowance or denial and the time recorded.
- [x] 3.2 Consult the store in the mode resolver for non-protected actions, and record in the transcript that a remembered decision resolved the call rather than showing an unasked request.
- [x] 3.3 Enforce origin scoping: no match across a different origin, including a different subdomain, and no match across a different action class.
- [x] 3.4 Write an entry only from an explicit user decision delivered through the decision channel; reject any path by which page content, tool output, or skill instructions could create one.
- [x] 3.5 Apply a remembered denial by refusing without asking, distinguishably in the transcript from a timeout and from a fresh denial.
- [x] 3.6 Add single-entry and revoke-all operations. ← (verify: a remembered decision is never stored or presented as an approval token, and existing token binding, single-use, and invalidation behavior is unchanged)

  NOTE on group 3 (updated — production wiring now lands): `host/agent/companion.js`'s real `createCanUseTool({...})` call now passes a live `policySnapshot: () => this._permissionPolicySnapshot()` getter (read fresh on every tool call, never captured at run start) and relies on `createCanUseTool`'s own default `persistSiteEntry` (site-store.js's `recordSiteEntry` — kept as the default INSIDE can-use-tool.js rather than a companion-supplied wrapper, because `host/test/site-store.test.mjs`'s import-graph guard restricts `recordSiteEntry` to that one file and tests; see that module's own doc comment). The SAME `policySnapshot` closure is also forwarded to the PreToolUse gate hook (`createPermissionModeGateHook`) in `buildIsolatedOptions()`, so both readers see one mode/managed-policy/site-store state, live. Local mode persistence is a new module, `host/agent/policy/mode-store.js` (atomic-write, default "auto"); `site-store.js`'s own write path was switched from a raw `fs.writeFileSync` to the same atomic-write helper (`host/agent/settings/atomic-store.js`) for crash safety, with no change to its tested read/write contract. Managed policy is held in `CompanionCore` in memory only (never persisted) and applied via a new wire message, `managed_policy_snapshot` (see group 4 below). A real, previously-undiscovered production gap was found and fixed while wiring this: `createCanUseTool`'s own body computed the mode resolver's "decide" outcome correctly but then silently discarded it for any ordinary MUTATING (non-send, non-protected) call — such a call's legacy `classifySendClassCall` verdict is "allow" (it isn't a send-class tool at all), and the code returned that "allow" immediately without ever checking whether the mode stage had said "decide". Manual mode's "every mutating action SHALL require a decision" was therefore unreachable even with full production wiring, for exactly the calls that are neither protected nor already send-class (e.g. `navigate`). Fixed in `can-use-tool.js`'s classification section: a `needsModeOverride` check forces the same approve-and-suspend path protected calls already use whenever the mode stage said "decide" but the legacy verdict alone would have said "allow" — proven not to change Auto or Skip mode's behavior (`needsModeOverride` can only ever be true when `modeDecision.outcome === "decide"` AND the legacy verdict is "allow", a combination Auto/Skip never produce for a non-protected class) and proven end-to-end in `host/test/permission-mode-companion-wiring.test.mjs`. `recordSiteEntry`'s only production importer is `can-use-tool.js`, exactly as the import-graph guard requires.

## 4. Managed policy channel

The host process cannot read `chrome.storage.managed` itself (it has no browser access) — the extension wave reads it and pushes a snapshot over a new wire message, `managed_policy_snapshot` (added to `host/agent/protocol.js`'s `AGENT_MESSAGE_TYPES`; contract recorded in `reports/wave1-contracts.md`). Everything below is the HOST half: receiving, validating, holding in memory, and applying that snapshot.

EXTENSION HALF DONE (this wave): `extension/managed-schema.json` (referenced from `manifest.json`'s new `storage.managed_schema` key — required for `chrome.storage.managed` to populate at all) declares the `mode`/`requireProtectedConfirm`/`sites` shape `validateManagedPolicy()` already expects. `extension/background.js` adds `readManagedPolicyForPush()`/`pushManagedPolicySnapshot()`: pushed once on every completed hello handshake (`envelope.type === "hello_ack"`) and again on every `chrome.storage.onChanged` for `area === "managed"`. An empty `chrome.storage.managed.get(null)` result (no way to distinguish "never configured" from "explicitly cleared" through that API) is pushed as `policy: null` (absent); a thrown read is pushed as `policy: null, readError: <message>`; a non-empty result is passed through verbatim for the host to validate. Covered by `test/background-managed-policy-and-downloads.test.mjs`.

- [x] 4.1 Read administrator policy from managed storage at the point each decision is made, not cached into local settings. `CompanionCore._handleManagedPolicySnapshot` validates and holds the latest snapshot in `this._managedPolicy` (in-memory field, never written to disk); `_permissionPolicySnapshot()` — the SAME live getter wired into `createCanUseTool`/the PreToolUse gate hook — reads `this._managedPolicy` fresh on every call, never a value captured once at run start.
- [x] 4.2 Support pinning the mode and placing sites into or out of the per-site store — both implemented and tested (`validateManagedPolicy`'s `mode`/`sites` fields, consumed by `_effectivePermissionMode()`/`_managedSiteMatch()` and the resolver's managed-site-match branch). "Requiring protected-action confirmation" (`requireProtectedConfirm`) validates for shape but is not separately consumed by the resolver: protected actions ALREADY require an explicit decision under every mode unconditionally (design.md decision 2 — protected classification is not policy-controlled), so there is no state for this flag to additionally produce when true, and task 4.5 requires the resolver to refuse ever letting it (or anything else) turn protected confirmation OFF. The field exists in the validated shape so a future stage has somewhere to read it, but functionally protected confirmation is already unconditional with or without it.
- [x] 4.3 Make managed values override local values while present, and make local values apply again immediately when the policy is withdrawn, with no managed value persisting as a local choice. `_applyManagedPolicy()` swaps `this._managedPolicy` and never writes it anywhere; withdrawal (`policy: null`) restores the local mode/site-store on the very next decision.
- [x] 4.4 Ignore a malformed policy, fall back to local settings, and report the condition as an unreadable administrator policy rather than treating it as absent. `_handleManagedPolicySnapshot` runs every non-null snapshot through `validateManagedPolicy`; a failure falls back to `null` for decisions but sets `_managedPolicyPresent=true`/`_managedPolicyReadable=false`/`_managedPolicyError=<joined errors>`, surfaced through the new `agent_settings` `get_permission_state` op's `managedPolicy` field. An extension-reported `readError` (it could not read `chrome.storage.managed` at all) is handled the same way.
- [x] 4.5 Reject any managed value that would remove a protected action's decision requirement. Structurally guaranteed, not merely validated: `resolveModeDecision()` checks `actionClass === PROTECTED` before it ever looks at managed policy (design.md decision 2's fixed one-directional order), and `validateManagedPolicy`'s `sites[].actionClass` is restricted to `REMEMBERABLE_CLASSES`, which excludes `protected` by construction — there is no field a managed policy could set to weaken a protected decision. ← (verified: `host/test/permission-mode-companion-wiring.test.mjs` withdraws a policy mid-session and confirms local behavior resumes on the next decision, and separately confirms a malformed policy is reported unreadable via `get_permission_state`, never silently absent)

## 5. Injection probe

- [x] 5.1 Add the probe over returned web content — extracted text, accessibility trees, and page-tool results — running before that content is available for the agent to act on. New `host/agent/threat/injection-probe.js` (`scanTextForInjection`/`probeToolResult`, heuristic pattern set) wired into `host/agent/threat/observe.js`, called from `host/agent/tools/adapter.js`'s `buildSdkTools()` immediately after `toolBridge.call(...)` returns and BEFORE the handler's `return result` — i.e. before the SDK hands the result back to the model. Probed tools (`PROBE_TOOL_NAMES`): `get_page_text`, `read_page`, `find`, `webmcp_list_tools`, `webmcp_call_tool` (all explicitly documented in `host/tool-definitions.js` as page-supplied/untrusted), and `browser_batch` (its aggregate result is what the agent actually receives for any of those tools run as a batch item, so it is "the tool that returned it" for the spec's "which tool returned it").
- [x] 5.2 Record a finding with the matched text, its location in the content, and the returning tool, and deliver the content unchanged. `probeToolResult` returns `{tool, tabId, field, patternId, matchedText, location:{start,end}}` per match; `observe.js` emits one `injection_finding` event per finding on the run's own event channel (`run.emit`, the same channel `approval_request`/`tool_rejected` use — see `host/agent/protocol.js`'s new `THREAT_EVENT_TYPES`). `adapter.js` always returns the original `result` object from `toolBridge.call` unchanged, regardless of probe outcome — proven byte-identical in `host/test/agent-threat-observe.test.mjs` ("the tool result content is delivered completely unchanged") and in `host/test/threat-advisory-only.test.mjs`.
- [x] 5.3 Carry matched text as quoted data everywhere it travels, including into the transcript, and ensure a finding never re-enters the model's context as prose. `matchedText` is always a plain object field on the emitted event, never interpolated into a template/prose string, in both `injection-probe.js` and `observe.js`. It travels into the transcript exactly the way every other `run.emit(...)` event already does (durable append in `CompanionCore`/`transcript-store.js`, untouched by this wave) and is never passed back into `result` or anywhere the model's next turn reads from — `adapter.js` returns the SDK-dispatched `result` verbatim, and no threat module writes to a run's conversation/message state.
- [x] 5.4 Record a probe failure distinguishably from a clean result, delivering the content and not blocking the run. `probeToolResult` returns `status:"failed"` (with an `error` string) for a real scanning exception, distinct from `status:"clean"`/`"finding"`/`"skipped"`; `observe.js` emits a separate `injection_probe_failed` event for it and returns early (no crash, no thrown error out of `observeToolResult`), and `adapter.js` additionally wraps the whole observation call in a belt-and-suspenders `try/catch` so an observer bug can never affect dispatch. Covered by `host/test/injection-probe.test.mjs` and `host/test/agent-threat-observe.test.mjs`.
- [x] 5.5 Keep findings out of the permission resolver's inputs. ← (verify: a finding on a page changes no decision outcome — the same action resolves identically with and without a finding present) Verified two ways in `host/test/threat-advisory-only.test.mjs`: (a) structurally, `host/agent/policy/authorization.js`, `can-use-tool.js`, `permission-modes.js`, and `host/agent/tools/mapping.js`/`query-options.js` are grepped and asserted to contain no reference to `threat/` at all; (b) behaviorally, the SAME protected action (`gif_creator` export+download) and the SAME ordinary mutating action (`navigate`) are dispatched twice through the real `buildSdkTools()`/`authorizeToolCall` path against the same tabId — once with a clean `TabRiskRegistry`, once with the identical tab already carrying a real recorded injection finding — and the two results are asserted byte-identical.

## 6. Per-tab risk category

- [x] 6.1 Derive a category for the controlled tab from observable signals, recording the contributing signals so they can be inspected. New `host/agent/threat/tab-risk.js` (`TabRiskRegistry`, categories `uncategorized`/`low`/`elevated`) and `host/agent/threat/content-signals.js` (credential/payment keyword heuristics). `observe.js` records one signal per observation (`credential_content`/`payment_content`/`content_reviewed`/`injection_finding`), each carrying `{kind, severity, label, ts, ...}`; `TabRiskRegistry.getState(tabId)`/the emitted `tab_risk_update` event's `signals` array make every contributing signal inspectable. Covered in `host/test/tab-risk.test.mjs`.
- [x] 6.2 Recompute on document identity change, reusing the identity the approval binding already tracks, and do not carry the previous document's category forward. Reuses the same minimal `{tabId, url}` shape `host/agent/policy/approvals.js`'s `docIdentity` binding already tracks (see that file and `companion.js`'s own comment on it) rather than inventing a new one. `TabRiskRegistry.observeDocument()`: the `navigate` tool is the explicit, unambiguous reset point (`observe.js` always calls it with `forceReset:true` for a `navigate` dispatch, even same-URL — a reload is still a new document); a later-observed different URL for an already-known tab (e.g. `get_page_text`'s own reported URL, for an in-page navigation not made through the `navigate` tool) resets passively; learning a URL for the first time is not treated as a replacement. A reset always clears both the category and the signal list — no carry-forward. Covered in `host/test/tab-risk.test.mjs` and `host/test/agent-threat-observe.test.mjs`.
- [x] 6.3 Represent insufficient signals as uncategorized, distinguishable from low risk. `TabRiskRegistry`'s default/reset state is `uncategorized` (zero signals); the first observation of any severity — including a merely-reviewed page with nothing elevated — moves it to `low`, a distinct value. Covered in `host/test/tab-risk.test.mjs` ("insufficient signals is distinguishable from low").
- [x] 6.4 Include injection findings among the contributing signals for the tab they came from. `observe.js` calls `tabRiskRegistry.recordSignal(tabId, {kind:"injection_finding", severity:"elevated", ...})` for every emitted `injection_finding`, so the finding appears in that tab's `signals` list and raises its category to `elevated`. Covered in `host/test/tab-risk.test.mjs` and `host/test/agent-threat-observe.test.mjs`.
- [x] 6.5 Keep the category out of the permission resolver's inputs. ← (verify: an elevated category changes no decision outcome, and no code path reads a category to decide whether an action proceeds) Same structural + behavioral proof as 5.5 in `host/test/threat-advisory-only.test.mjs`, this time seeding the shared `TabRiskRegistry` to `elevated` (a real recorded finding, not a fabricated field) before dispatching the protected and the ordinary mutating action — both resolve byte-identically to the clean-registry run.

NOTE on groups 5/6 (event contract): `injection_finding`, `injection_probe_failed`, and `tab_risk_update` are new inner `event.type` values carried inside the existing `stream_event` envelope — no new envelope/message type was needed (`AGENT_MESSAGE_TYPES` in `host/agent/protocol.js` is unchanged); they are named centrally as `THREAT_EVENT_TYPES` in that same file purely so the host and the panel wave share one vocabulary instead of two independently-typed magic strings, exactly like `CHUNK_KINDS` already does for a different concern. Every emission goes through the run's own `run.emit(...)`, so it is forwarded live and appended to the durable per-conversation transcript the same way `approval_request`/`run_error`/`tool_rejected` already are — there is no separate "outstanding request" registry the way there is for `approval_request`/`question_request`, since a finding or a category update is a fact, never something waiting on a reply. `TabRiskRegistry` is shared by every tool call built off ONE `buildSdkTools()` call, so per-tab state and document-identity reset both work correctly within a single run. Every tool call within that run shares it, so per-tab state and document-identity reset both work correctly for the lifetime of one run. Exact event/payload shapes are recorded in `reports/wave2h-events.md` for the panel wave.

  Follow-up fix (CRITICAL, closed this pass): the sentence this note used to end on — "It does not persist a tab's category across separate runs/turns of the same conversation" — was a real, reproduced bug, not just an acknowledged gap: `buildSdkTools()`'s own default (`tabRiskRegistry ?? new TabRiskRegistry()`) built a brand-new, empty registry on every call, and `companion.js`'s sole call site never supplied one of its own, so every `startRun()` silently discarded the previous turn's findings for a tab that never navigated — reading back as `uncategorized`, the exact state the spec reserves for "no signal has ever been observed". Fixed by giving the registry a companion-level, cross-run home now that `companion.js` is in scope: a new `_tabRiskByConversation` map (keyed by conversationId) plus `_tabRiskRegistryFor(conversationId)`, lazily creating one `TabRiskRegistry` per conversation and reusing it for every later run of that SAME conversation, passed into `createBrowserMcpServer()` instead of leaving it defaulted; cleared on `_handleDeleteConversation` so it does not linger forever. `TabRiskRegistry.observeDocument()` itself is completely unchanged — a `navigate` call or an observed URL change still resets a tab's category/signals exactly as before; only how long "no change yet" is allowed to mean "carry the previous state forward" changed, from one run to the whole conversation. Covered by `host/test/tab-risk-conversation-persistence.test.mjs` (a tab's elevated category and its signals survive a second `startRun()` on the same conversation when the document has not changed, and still reset to `uncategorized` on an explicit `navigate`).

## 7. Panel and settings surfaces

- [x] 7.1 Add the mode control showing the active mode wherever a run can act, with a pinned mode rendered as administrator-controlled and locally unchangeable.

  DONE (extension wave). A mode badge (`<ui-menu id="mode-menu-wrap">` in `extension/sidepanel/sidepanel.html`, wired in `extension/sidepanel/sidepanel.js`) sits in the chat panel's header — visible wherever a run can act — reading/writing through the new `extension/sidepanel/permissions-client.js` (`get_permission_state`/`set_permission_mode` ops). A parallel, fuller control lives on the new `extension/settings/permissions.html` page (`extension/settings/permissions-controller.js` + `permissions-app.js`, linked from `settings.html`'s "Quyền & trang đã ghi nhớ" row). Both read `deriveModeView()` (`extension/ui/permission-labels.js`, shared, pure): when `modeSource === "managed"` the trigger is disabled, shows a lock icon, and the menu renders no items at all — never merely visually de-emphasized. A `MANAGED_POLICY_PINNED` reply from `set_permission_mode` (e.g. a policy applied after the badge last loaded) re-syncs the badge to "managed" and renders no error toast — never surfaced as a generic failure. Covered by `test/permission-labels.test.mjs` (the pure view derivation) and `test/settings-permissions-controller.test.mjs` (the controller's handling of `MANAGED_POLICY_PINNED`, never-attempt-when-managed, and effective-change detection).

- [x] 7.3 Extend the decision card: name the protected category and state that the decision cannot be remembered for protected actions; offer remembering for every other card, recording an entry only when the user asks. HOST SIDE DONE (prior wave). PANEL/UI HALF DONE (this wave): `extension/sidepanel/conversation-model.js`'s `pendingApproval` now carries `protectedCategory`/`rememberable` straight from the `approval_request` event. `extension/sidepanel/sidepanel.js`'s `renderPermission()` names the protected category (`protectedCategoryLabel()`, `extension/ui/permission-labels.js`) and states plainly it will not be remembered when `protectedCategory` is set; for every other card it renders a "remember this decision" checkbox (only when `rememberable === true`) and passes the checked state through `panel.respondApproval(decision, { remember })`. `extension/sidepanel/panel-controller.js`'s `respondApproval()` forwards `remember` to `extension/sidepanel/protocol-client.js`'s `approvalDecision()`, which sends the `remember` field on the wire ONLY when it is `true` (never `false`, matching the wire contract's "ONLY when the user asked for it"). Covered by `test/permission-mode-panel-wiring.test.mjs`.

- [x] 7.2 Invalidate and clear an outstanding decision card on a mode change, rejecting a late answer to it. HOST SIDE DONE (prior wave). PANEL/UI HALF DONE (this wave): `PanelController.invalidateAllPendingApprovals()` clears `pendingApproval` on EVERY conversation model this panel currently holds (a mode change is companion-process-wide, not scoped to the visible tab) the instant the panel's own mode-change request reports the effective mode actually changed — rather than waiting for the host's `rejectAll()` denial to round-trip back as an ordinary tool result. Wired from both mode controls (the sidepanel badge's `changePermissionMode()` and, for the settings page, the caller compares `PermissionsController.setMode()`'s returned "did it actually change" boolean). Clearing the card also closes the late-answer race: `respondApproval()` already no-ops when `pendingApproval` is `null`, so a stale click can no longer send a decision for a request the host has already forgotten. Covered by `test/permission-mode-panel-wiring.test.mjs` ("a late reply to an already-cleared approval is a no-op" / "invalidateAllPendingApprovals() clears every open conversation's card").
- [x] 7.4 Add warning surfaces for findings and risk categories, carrying no allow or deny control and suspending nothing, visually and semantically distinct from a decision card.

  DONE (extension wave 3). `extension/sidepanel/conversation-model.js`'s `applyEvent()` records `injection_finding`/`injection_probe_failed`/`tab_risk_update` as turn-anchored `turn.warnings[]` entries (never touching `pendingApproval`/`pendingQuestion`) and a `tabRisk` Map keyed by tabId (`getTabRisk(tabId)`) for the latest known category. `extension/sidepanel/sidepanel.js`'s `warningRowHtml()`/`renderWarningsHtml()` render them with `role="status"` and NO `<button>` anywhere, using their own `.threat-warning*` CSS (`extension/ui/components.css`) sharing no class with `.permission-card`. Covered by `test/sidepanel-conversation-model.test.mjs` (model-level, including reconnect dedup) and `test/sidepanel-threat-warnings.test.mjs` (rendered HTML has no button/alertdialog).

- [x] 7.5 Display matched injected text as quoted data, so markup or instructions inside it are neither rendered nor acted on.

  DONE. New pure module `extension/ui/threat-labels.js` (`viewWarning()`/`viewRiskContext()`) returns every page-authored string (a finding's `matchedText`, a signal's `matchedText`) verbatim under a `quoted*`-prefixed key, documented as MUST-render-inert; `signals[].label` (host-authored) is a separate, safe-to-render-directly field. `sidepanel.js` renders every `quoted*` field via `escapeHtml()` interpolated as plain text inside a `<code>` block — the SAME technique already used for tool-result page content (`toolRowHtml()`'s `resultSummary`) — and deliberately never through `renderMarkdownLite()`, so `[link](https://...)`/`**bold**`/`<script>`/`<img onerror>` inside matched text never become real markup, links, or formatting. Proven by `test/threat-labels.test.mjs` and `test/sidepanel-threat-warnings.test.mjs` (a hostile matched-text fixture produces no `<script>`, `<img>`, or `<a href>` in the rendered output, with a control-check proving `renderMarkdownLite` WOULD have produced a link if wrongly used).

- [x] 7.6 Show relevant finding and risk context on a decision card when one is raised for that tab.

  DONE. `viewRiskContext()` (`extension/ui/threat-labels.js`) returns context only when the target tab's CURRENT category is `"elevated"` (an injection finding always drives a tab to elevated — reports/wave2h-events.md — so this one condition also covers "the tab has a live finding"; per the same report, category resets on navigation, so a stale finding from a prior document is correctly not carried forward as false context). `sidepanel.js`'s `renderPermission()` looks up `model.getTabRisk(target.tabId)` and renders `permissionRiskContextHtml()` — a pure function, unit-tested in isolation — inside the SAME card, with no control of its own; the card's own Allow/Deny remain the only controls. Covered by `test/threat-labels.test.mjs` and `test/sidepanel-threat-warnings.test.mjs`.

- [x] 7.7 Surface the controlled tab's category on the tab itself through the existing overlay notice.

  DONE end to end. `extension/overlay/pointer-overlay.js` (prior wave): a `riskUpdate` synthetic event in `reduceOverlayState()` (mirrors the existing `approval`/`keepalive` pattern: refreshes liveness, never touches cursor/click/step state, drops an unrecognized category rather than accepting it verbatim) and a `riskCategory`/`riskLabel` pair in `computeRenderModel()`'s output, defaulting to `"uncategorized"` — never fabricated as `"low"` — whenever no real signal has been seen yet, matching the host's own default and the spec's "uncategorized... distinguishable from a category of low risk". A `.browzy-risk` chip (no listener, not a button) renders it in the status bar, and a `browzyOverlayRisk` message type is wired into `onOverlayMessage()`, mirroring `browzyOverlayApproval`'s relay shape.
  The remaining hop is now closed: `extension/background.js`'s `handleAgentMessage()` already sees every `stream_event` envelope from the native host in transit (the same place the pre-existing `approval_request`/`run_started` overlay hooks observe them), so a `tab_risk_update` inner event is observed there too and handed to a new `forwardRiskUpdateToOverlay(event)` (next to `forwardApprovalToOverlay`), which sends `{type:"browzyOverlayRisk", tabId, category}` to the EXACT tab the event names via the existing `sendOverlayMessage()` — never to a run's other known tabs and never to whatever tab is active, and never with `signals`/`matchedText` (page-authored quoted data the chip has no use for). A relay failure (no overlay mounted, tab gone) is swallowed exactly like every other overlay message. A reset to `"uncategorized"` after navigation is relayed too, so the chip clears rather than showing a stale elevated state. Covered by `test/overlay-risk-relay.test.mjs` (new: `forwardRiskUpdateToOverlay` unit tests plus a `handleAgentMessage` structural test proving the hook fires only for `tab_risk_update` and never disturbs the pre-existing verbatim relay to `agentPorts`) and `test/overlay-pointer.test.mjs` (unchanged, pure-function/wiring tests on the overlay side).
- [x] 7.8 Add the approved-sites management page listing origin, action class, allowance or denial, and time recorded, with per-entry and revoke-all controls, administrator entries marked and non-revocable, an explicit empty state, and a statement that protected actions are never remembered. ← (verify: a warning surface offers no decision control anywhere in the panel, and acknowledging one authorizes nothing — true here: this page has no warning surfaces at all, only mode + revoke controls)

  DONE (extension wave). New page `extension/settings/permissions.html` (`extension/settings/permissions-app.js` DOM glue over the DOM-free `extension/settings/permissions-controller.js`), linked from `settings.html`. Lists every entry from `get_permission_state().sites` (managed first, per host order) with origin/action-class/allow-or-deny/recorded-at via `viewSiteEntry()` (`extension/ui/permission-labels.js`); a managed entry (`source:"managed"`) is marked administrator-controlled with no revoke button, a local entry gets one, wired to `revoke_site_entry`. A `NOT_FOUND` reply leaves the list unchanged and shows an error banner — never removed as if the revoke had succeeded. `MANAGED_ENTRY_LOCKED` renders as an informational (not generic-failure) banner. Revoke-all (`revoke_all_site_entries`, with an in-page press-again confirmation) removes only local entries client-side, matching the host's own "managed sites are unaffected" semantics; it is disabled outright when every visible entry is managed. An explicit empty state (`#sites-empty`) renders when the list is empty, distinct from a bare empty list. A standing note above the list states protected actions are never remembered. Covered by `test/settings-permissions-controller.test.mjs`.

## 8. Documentation

- [x] 8.1 Document the three modes, what each gates, and that Auto is the default and matches prior behavior. Added `README.md`'s new "Permission modes and protected actions" section (`### The three modes`), verified against `specs/agent-permission-policy/spec.md`'s mode requirement and `reports/wave1-contracts.md`. Follow-up (this pass): task 7.1's mode control has since landed — verified `extension/sidepanel/sidepanel.js`'s mode badge/menu (`renderModeMenu`/`changePermissionMode`) and `extension/settings/permissions.html`'s mode radio group — so the section now adds a "Where to change it" paragraph naming both surfaces instead of staying silent on the panel click path.
- [x] 8.2 Document the protected-action categories and that no mode, remembered decision, or administrator policy can bypass them. Added the same section's `### Protected actions`, cross-checked against the spec's protected-action requirement and task 2.1–2.6's evidence in this file. Follow-up (this pass): task 2.4's download gate has since landed — verified `extension/manifest.json` now carries `downloads`, and `extension/background.js`'s `handleDownloadCreated`/`handleDownloadDecision` implement the pause/decide/resume-or-cancel flow, gated on `isAgentRunActive()` (an agent run currently holding the browser; a user's own manual download is never gated) — so the section now describes the gate as shipped, including its one honest limit: a download the browser finishes before it can be paused is reported (`download_notice`) rather than blocked.
- [x] 8.3 Document the administrator policy keys and their precedence over local settings. Added `### Administrator-managed policy`, using `reports/wave1-contracts.md`'s exact `validateManagedPolicy` shape as ground truth rather than re-deriving it, and stating `requireProtectedConfirm`'s honest status verbatim from that report: validated for shape, not separately consumed, because protected actions already require a decision unconditionally, so the key can only ever add a confirmation, never remove one. Unchanged this pass.
- [x] 8.4 Update the privacy and store-listing material for the `downloads` permission being added to the manifest, stating it is used only to observe download creation and to pause/resume/cancel a download pending the user's decision — never to start, redirect, or read downloads. Added `docs/privacy-policy.md`'s "Quyền downloads (đang được bổ sung)" section with that exact scope. Follow-up (this pass): re-verified `extension/manifest.json` now has the `downloads` entry and `extension/background.js` implements the pause/decide/resume-cancel gate described above; renamed the section to "Quyền downloads" (dropped "đang được bổ sung"), removed the "chưa có trong manifest" sentence, and added the same agent-run-scoped and completed-before-pause disclosures as 8.2 (in Vietnamese, matching this document's voice), bumping the file's own "Cập nhật lần cuối" convention. Updated `README.md`'s cross-link anchor from `#quyền-downloads-đang-được-bổ-sung` to `#quyền-downloads` to match. No store-listing file (e.g. a Chrome Web Store draft description) was found in the repository to update; only `docs/privacy-policy.md` exists as a store-adjacent surface.

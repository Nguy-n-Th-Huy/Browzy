## 1. Subgoal mode in the Jev runtime

- [x] 1.1 In `host/agent/jev/runtime.js`, add a `mode: "subgoal"` path to `runTypesafeRun` (via `limits.mode` or an explicit param). Map subgoal budgets from `limits` (smaller `maxActions`/`maxDecisions`/no-progress, and `maxMemoryUpdates` 1–2), defaulting to bounded subgoal constants.
- [x] 1.2 In subgoal mode, KEEP `prepare("start")` and bounded `replan(...)` — they are the content preparation that produces `prepared.textValues`/`navigation` (skipping them would make non-literal typing and planned navigation impossible). The returned `memory.plan`/`doneWhen` feeds only content prep and Jev's `goal_done` monitor, never an LLM completion verifier. The operator-literal fast path (sourced from `provider.goal`) and text-model TYPE_TEXT generation both remain.
- [x] 1.3 In subgoal mode, end at Jev DONE or a positive `goal_done` as an unverified checkpoint — do NOT call `requestCompletionCheck`/`requestFinalReport` (this is the only behavioral subtraction vs standalone). ASK ends blocked with needsOperator. Return a structured result the caller can map (terminal reason, observed url/title, ordered action summary with TYPE_TEXT value omitted).
- [x] 1.4 Tag emitted `jev_step`/`jev_end` events as subgoal-originated (e.g. `subgoal: true` + a subgoal id) without changing standalone-run event shape.
- [x] 1.5 Confirm no standalone (`typesafe`) run behavior changes: same plan/verify/report path when `mode` is not subgoal ← (verify: standalone runtime tests unchanged and green; subgoal mode skips only planning+verification, all guards intact)

## 2. The browser_subgoal SDK tool

- [x] 2.1 Add a host-implemented `browser_subgoal({ goal })` tool (in the SDK extra-tools surface, alongside ask-the-user/create-document). Validate `goal` is a non-empty string; reject otherwise with a bounded validation failure and no sub-run.
- [x] 2.2 The handler builds the sub-run `provider` from the new profile resolver (task 3.0): Jev transport source + endpoint (`typesafeDefaultForSource`, never `profile.baseUrl`) + apiKey, `openai` decision model, textModel + apiKey, `tabId` = outer bound tab, `sendScreenshots`, minimal `conversation`; then calls `runTypesafeRun` in subgoal mode sharing the outer `run`, `toolBridge`, `coerceArgs`, and `canUseTool`.
- [x] 2.3 Map the sub-run result to the unverified-checkpoint contract: `{ unverified: true, url, title, actions[], reason, needsOperator? }` on success/blocked; a bounded host-authored named failure (no secrets) when the sub-run cannot start/run (bad key, capability not proven, transport failure). The outer run continues in all cases.

## 3. Config resolution and conditional registration

- [x] 3.0 In `host/agent/settings/profile.js`, add ONE new exported resolver that returns the resolved Jev config for an `anthropic`/`chatgpt` profile — transport source + endpoint (`typesafeDefaultForSource(resolveTypesafeSource(profile))`, never `profile.baseUrl`) + typesafe apiKey, the `openai` decision model (base URL + model id + key via `resolveTypesafeDecisionModel`/`readTypesafeKeys`), and text model + apiKey — or `null` when the gate is unmet. No persisted-shape change. ← (verify: resolver returns null unless decision source is `openai` with all keys present; endpoint never comes from `profile.baseUrl`)
- [x] 3.1 In `host/agent/companion.js`, register `browser_subgoal` in the `createBrowserMcpServer` `extraTools` for `anthropic`/`chatgpt` runs ONLY when the task 3.0 resolver returns non-null (decision source `openai` + all keys + text model). Absent/ineligible config → tool not registered, no error. Never register it for a `typesafe` run.
- [x] 3.2 Settings confirmed no host change: `set_typesafe_config`/`set_typesafe_credentials` (companion.js ~2753-2823) and `setTypesafeConfig`/`setTypesafeCredentials` (profile.js ~748-963) already persist Jev config on any profile with no `providerType` gate. Make no settings/schema change; note the extension-side UI hiding of these fields for non-`typesafe` types as out of scope.

## 4. Tests

- [x] 4.1 New `host/test/jev-browser-subgoal.test.mjs`: tool present when the resolver is eligible (decision source `openai` + all keys), absent (no error) when any field missing OR decision source is `anthropic`/`chatgpt`, absent for a `typesafe` run; `goal` validation rejects empty/non-string; checkpoint result shape marked unverified with TYPE_TEXT value omitted; bad-key/capability/transport failure returns a bounded named failure and the outer run continues.
- [x] 4.2 Subgoal-mode runtime tests (in the existing Jev runtime suite): subgoal mode still prepares content via one bounded model call but runs NO completion verification/report; DONE or positive `goal_done` ends as an unverified checkpoint; a send-class action still suspends on the shared approval card; denied approval ends blocked; a bound exhaustion (incl `maxMemoryUpdates` 1–2) ends blocked with its named reason; ASK ends needsOperator; subgoal `jev_step` events are tagged distinctly ← (verify: all guards fire in subgoal mode exactly as standalone; completion verify/report is the only step skipped)
- [x] 4.3 Run `node --test host/test/jev-*.test.mjs` plus any snapshot/registry tests touched, and the repo's fuller test trees. Report failures in unowned files (parallel-session work) without editing them ← (verify: in-scope tests green; pre-existing out-of-scope failures reported with ownership noted)

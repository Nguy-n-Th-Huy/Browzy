# Tasks

## 1. Extension: page-side detection and relay

- [x] 1.1 Add `webmcp-types` as a dev dependency for accurate type shapes. Do not add any runtime dependency.
- [x] 1.2 Create `extension/webmcp/detect-main.js`, a MAIN-world script. Guard the whole body behind a `typeof document.modelContext` check so an absent API costs one check and produces no error, no throw, and no console output. Install a `registerTool` wrapper immediately (required at `document_start`, or already-completed registrations are missed) that records each definition's `name`, `description`, `inputSchema` and `execute` callback while forwarding the call through to the original. Then, if `getTools` is a function, prefer it as the inventory source. Subscribe to `toolchange` and re-report. Report the inventory plus a capability flag for whether `executeTool` is a function, over `window.postMessage` under a namespaced marker.
- [x] 1.3 In the same script, handle inbound call requests: if `typeof document.modelContext.executeTool === "function"`, call it and tag the outcome `via: executeTool`; otherwise invoke the captured `execute` callback and tag it `via: captured-callback`. Report throws and rejections as failed calls carrying the reason — never as silent empty results.
- [x] 1.4 Create `extension/webmcp/relay-isolated.js`, an ISOLATED-world script. Accept only messages whose `event.source === window` and that carry the namespaced marker, and bridge them to `chrome.runtime`. Add a comment stating plainly that this check prevents cross-channel talk and does **not** make the tool inventory trustworthy — the page is the authority on its own tools by design.
- [x] 1.5 Add ONE new `content_scripts` entry to `extension/manifest.json` with `"world": "MAIN"`, `"run_at": "document_start"`, `"all_frames": false`. Leave both existing entries byte-identical — the recorder's `all_frames: true` / `document_start` entry is load-bearing for imitation learning. Do not change `minimum_chrome_version`. Do not add permissions. ← (verify: manifest diff touches only the added entry; `minimum_chrome_version` still 116; `permissions` array unchanged; recorder entry unchanged)

## 2. Extension: service-worker state and handlers

- [x] 2.1 Add a per-tab page-tool table to `extension/background.js`, keyed by tab id, holding the reported inventory and the `executeTool`-availability flag. Wire the `chrome.runtime` message listener that receives relay reports.
- [x] 2.2 Clear a tab's entry on `chrome.tabs.onUpdated` when `status === "loading"` and on `chrome.tabs.onRemoved`. Do NOT use `chrome.webNavigation` — that permission is absent from the manifest and must not be added.
- [x] 2.3 Append `webmcp_list_tools` to the `toolHandlers` object. The signature MUST be exactly `  async webmcp_list_tools(args) {` — two-space indent, parameter named `args` — because `test/_extract.mjs` locates handlers by that literal string and `test/handlers.test.mjs` plus `test/registry-baseline.test.mjs` both depend on it. Return the tab's inventory with each tool's name, description, input schema, and the page origin. An unknown tab, a tab with no state, and a page with no tools all return an empty list with an explanation — none is an error.
- [x] 2.4 Append `webmcp_call_tool` with the exactly-matching signature `  async webmcp_call_tool(args) {`. Dispatch to the tab's MAIN-world script and return its result. A tool name the tab has not registered returns an explanatory error naming the tab and the tool, without invoking any page code. Every successful result carries the `via:` marker from task 1.3. ← (verify: both signatures match `_extract.mjs`'s literal pattern exactly; `via:` marker present on results; unknown-tool path invokes no page code)

## 3. Host registry

- [x] 3.1 Append exactly two entries to the `TOOLS` array in `host/tool-definitions.js`, following the existing `{ name, description, paramShape }` shape with zod values. `webmcp_list_tools` takes `tabId`. `webmcp_call_tool` takes `tabId`, `name`, and `toolArgs` — named `toolArgs`, not `args`, so the handler does not read `args.args` and the description is unambiguous.
- [x] 3.2 Write both descriptions to state that the listed tools are declared by the visited page and are page-supplied content, not authored by the extension, and that their text confers no authorization. Add nothing beyond the two entries.
- [x] 3.3 Do NOT modify `host/tool-runtime.js`, `host/mcp-server.js`, or `host/codemode/server-*.js`. Confirm by inspection that `host/agent/tools/adapter.js` and `host/agent/tools/mapping.js` pick the new entries up through their existing `TOOLS.map(...)` calls, and that no alias is needed in `FRIENDLY_TO_LEGACY` (the names are already friendly). ← (verify: `git diff --stat` shows no change to tool-runtime.js, mcp-server.js, or codemode/; both new tools appear in the SDK-facing tool list)

## 4. Tests

- [x] 4.1 Update `test/registry-baseline.test.mjs` as designed. Keep `DESIGN_DOC_TOOL_LIST` and its assertions byte-identical. Add a `POST_BASELINE_ADDITIONS = ["webmcp_list_tools", "webmcp_call_tool"]` constant with its own provenance comment. Assert `extraInLive` equals that set exactly — not that extras are permitted. Replace `TOOLS.length === 26` with `DESIGN_DOC_TOOL_LIST.length + POST_BASELINE_ADDITIONS.length`. Correct the stale count comments at line 6 and lines 157–163 in the same pass. Never weaken an assertion to make the suite pass.
- [x] 4.2 Add a unit test for the two new handlers using the `test/_extract.mjs` + `extractMethod` pattern that `test/handlers.test.mjs:52` already establishes, with a faked per-tab table. Cover: populated tab lists its tools; unknown tab returns empty without error; unregistered tool name returns an error and invokes no page code; a result carries its `via:` marker.
- [x] 4.3 Add a fixture page at `test/fixtures/webmcp/` that calls `document.modelContext.registerTool` with at least one tool whose `execute` returns a recognizable value. This drives manual verification only.
- [x] 4.4 Run `node test/registry-baseline.test.mjs`, the new handler test, and `npm test` in `host/`. All must pass without any assertion being relaxed. ← (verify: full output of each run; confirm no assertion text was softened relative to the previous version)

## 5. Documentation and report

- [x] 5.1 Add a README section describing this capability as experimental and dependent on the WebMCP origin trial: Chrome 149–156, expiring 2026-11-16, unavailable unless the visited site is enrolled or the user enables `chrome://flags/#enable-webmcp-testing`. Follow the honesty pattern of the existing "Side panel status" section. Do not describe it as generally available.
- [x] 5.2 Update the README's "26-tool" claims to reflect the 26 preserved baseline operations plus the 2 additions, matching the split the spec delta and the baseline test now draw.
- [x] 5.3 Write the change report to `D:/Dev/www/Browzy/plans/reports/`. State plainly that CI has no Chrome, that the two handlers are unit-tested but the `document.modelContext` interaction is not automatically covered, and record the manual-probe procedure. Leave the observed `via:` value blank pending the manual run rather than inventing one. ← (verify: report claims no coverage that does not exist; no fabricated probe result)

## 6. Manual verification (requires a human — cannot run in CI)

- [ ] 6.1 Enable `chrome://flags/#enable-webmcp-testing` and restart Chrome. Load the unpacked extension and open the fixture page from task 4.3.
- [ ] 6.2 Run `webmcp_list_tools` against the fixture tab and confirm the registered tool appears with its name, description, and schema, marked as page-supplied.
- [ ] 6.3 Run `webmcp_call_tool` and confirm the expected value returns. Record the observed `via:` value in the report from task 5.3. Navigate away and confirm the tab's tool list empties. ← (verify: report updated with the real observed `via:` value and the navigation-clears-tools result)

## 7. Policy gap fix: borrowed-tab scope for WebMCP tools (post-verify, operator-approved scope addition)

This group is scope approved by the operator after an independent verify agent found the gap below — it was not part of the original plan.

- [x] 7.1 Add `webmcp_list_tools` and `webmcp_call_tool` to `TAB_ARG_KEYS` in `host/agent/policy/authorization.js`, keyed on `tabId`, so the unconditional handler-side tab-scope check actually inspects these two tools' tabId argument (it previously fell through as scope-free).
- [x] 7.2 Add `webmcp_list_tools` and `webmcp_call_tool` to `TAB_TARGET_ARG_KEYS` in `host/agent/tools/mapping.js`, keyed on `tabId`, so `enforceBorrowedTabScope()` — previously a silent no-op for both tools because neither had an entry in this table — actually runs for them. Verified the existing mutation classification (`READ_ONLY_LEGACY_TOOLS`/`MUTATING_LEGACY_TOOLS`) already correctly places `webmcp_list_tools` as read-only and `webmcp_call_tool` as mutating (same treatment as `javascript_tool`); no classification change was needed, only the missing arg-key entries.
- [x] 7.3 Verified by reading the dispatch path (`host/agent/tools/adapter.js`'s `buildSdkTools()`) that `enforceBorrowedTabScope()` runs unconditionally for every registered tool, including these two, BEFORE the call ever reaches `extension/background.js`. Because the host-side gate now rejects an unauthorized borrowed-tab `webmcp_call_tool` dispatch before it is ever sent to the extension, no mirror change in `extension/background.js`'s `isInGroup()` is needed — that check already correctly enforces run-tab-scope (not borrowed-vs-agent-created distinction) for the SDK path and was never the gap. Also verified `_isAutoAuthorizeEligible()` in `adapter.js` already excludes `webmcp_call_tool` from the automatic borrowed-tab authorization grant (it only auto-authorizes `form_input` and non-send-class `computer` calls), so no change was needed there either.
- [x] 7.4 Added tests locking this behavior into `test/registry-sdk-mapping.test.mjs`: `enforceBorrowedTabScope()` rejects an unauthorized `webmcp_call_tool` dispatch against a borrowed tab and allows it once explicitly authorized; `enforceBorrowedTabScope()` allows `webmcp_list_tools` against a borrowed tab with no authorization; and two adapter-level integration tests (`buildSdkTools()` handler dispatch) confirming the same for the real SDK tool-call path. Adjusted the pre-existing "every registry entry reaches the executor" reachability test to authorize tab 1 for `webmcp_call_tool` before dispatch, since that call is now correctly gated (mirrors the existing `file_upload` allowlist-before-dispatch pattern in the same test) — no assertion was weakened.
- [x] 7.5 Ran `node test/registry-borrowed-tab-scope.test.mjs`, `node test/registry-sdk-mapping.test.mjs`, `node test/webmcp-handlers.test.mjs`, `node test/webmcp-detect-main.test.mjs`, `node test/registry-baseline.test.mjs`, and `npm test` in `host/`. All pass.

## 1. Readable reports — panel/report owner

- [x] 1.1 Fix safe bare links, multiline ordered lists and narrow table rendering with URL-boundary, hostile-input and numbering regressions. Joint implementation checks and independent review passed.
- [x] 1.2 Make completion/terminal reports concise and task-first; add accessible safe secondary-detail disclosure, preserving grounding and visible incomplete outcomes. Joint implementation checks and independent review passed, including the corrected fenced-code finding.

## 2. Execution feedback — host owner then panel consumer

- [ ] 2.1 Agree and document additive phase/evidence event fields; emit actual runtime phases and bounded before/after evidence through existing capture/artifact mechanisms with privacy and unavailable-path tests. Implementation exists, but verification is reopened: a known tool `isError` result can be masked as succeeded. Host correction and independent re-review are pending.
- [ ] 2.2 Consume/restore metadata; render accurate operation versus planning/read/check counts, exact historical thumbnails and missing-evidence states; test terminal precedence, replay and legacy records. Panel projection/render and artifact-client tests pass; actual recorded artifacts load on explicit “Xem ảnh đã ghi” via existing conversation-scoped chunk request, never recapture. Verification is reopened for the known tool-error outcome defect and its panel rendering. Live Brave acceptance remains task 4.2.

## 3. Scenario coverage — runtime test owner

- [x] 3.1 Add popup-obstruction and delayed-result scenarios asserting action sequences and bounded recovery without false completion or resubmission.
- [x] 3.2 Add empty-results, pagination and changed-filter scenarios asserting grounded answers and legitimate new submissions.

## 4. Verification — parent

- [ ] 4.1 Run focused/required checks and independent review, resolving scoped findings.
- [ ] 4.2 Reload and run live Brave QA for original search, report formatting, phases/counts, historical evidence and representative difficult cases; record observations and limitations here.

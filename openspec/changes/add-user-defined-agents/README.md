# add-user-defined-agents

User-defined AI Agents catalog: import/author named agent definitions, pass them programmatically to the SDK's agents option, pick a primary agent per conversation, with tool-permission intersection that can never widen the run's authority.
